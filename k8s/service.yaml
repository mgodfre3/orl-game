apiVersion: v1
kind: Service
metadata:
  name: mario-clone-service
  namespace: mario-gitops
spec:
  type: LoadBalancer
  loadBalancerIP: ${LOADBALANCER_IP}
  selector:
    app: mario-clone
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
